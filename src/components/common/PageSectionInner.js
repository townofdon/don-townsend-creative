
import React from 'react';

import './PageSectionInner.scss';

const PageSectionInner = ({
  children,
}) => (
  <div className="page-section-inner">
    {children}
  </div>
);

export default PageSectionInner;
